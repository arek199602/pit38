# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html
# transaction_type values must be valid (matches the DB CHECK constraint).

# == Schema Information
#
# Table name: transactions
#
#  id               :bigint           not null, primary key
#  broker           :string           not null
#  currency         :string           not null
#  fee              :decimal(18, 8)   default(0.0), not null
#  price            :decimal(18, 8)   default(0.0), not null
#  quantity         :decimal(18, 8)   default(0.0), not null
#  ticker           :string           not null
#  transacted_on    :date             not null
#  transaction_type :string           not null
#  created_at       :datetime         not null
#  updated_at       :datetime         not null
#
# Indexes
#
#  index_transactions_on_ticker_and_transacted_on  (ticker,transacted_on)
#
aapl_buy:
  broker: trading212
  transaction_type: buy
  transacted_on: 2024-03-15
  ticker: AAPL
  quantity: 10.0
  price: 172.50
  currency: USD
  fee: 0.0

aapl_sell:
  broker: trading212
  transaction_type: sell
  transacted_on: 2024-09-20
  ticker: AAPL
  quantity: 10.0
  price: 195.00
  currency: USD
  fee: 0.15
