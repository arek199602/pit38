# PostgreSQL. Versions 9.3 and up are supported.
#
# Install the pg driver:
#   gem install pg
# On macOS with Homebrew:
#   brew install postgresql
#
default: &default
  adapter: postgresql
  encoding: unicode
  # For details on connection pooling, see Rails configuration guide
  # https://guides.rubyonrails.org/configuring.html#database-pooling
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: pit38_development

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: pit38_test

# Production uses separate databases for the Solid adapters (cache/queue/cable).
production:
  primary:
    <<: *default
    database: pit38_production
    username: pit38
    password: <%= ENV["PIT38_DATABASE_PASSWORD"] %>
  cache:
    <<: *default
    database: pit38_production_cache
    username: pit38
    password: <%= ENV["PIT38_DATABASE_PASSWORD"] %>
    migrations_paths: db/cache_migrate
  queue:
    <<: *default
    database: pit38_production_queue
    username: pit38
    password: <%= ENV["PIT38_DATABASE_PASSWORD"] %>
    migrations_paths: db/queue_migrate
  cable:
    <<: *default
    database: pit38_production_cable
    username: pit38
    password: <%= ENV["PIT38_DATABASE_PASSWORD"] %>
    migrations_paths: db/cable_migrate
